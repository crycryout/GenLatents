diff --git a/generate.py b/generate.py
index 3a5cbcd..ea469de 100644
--- a/generate.py
+++ b/generate.py
@@ -2,9 +2,11 @@
 import argparse
 import logging
 import os
+import socket
 import sys
 import warnings
 from datetime import datetime
+from pathlib import Path
 
 warnings.filterwarnings('ignore')
 
@@ -161,6 +163,11 @@ def _parse_args():
         type=str,
         default=None,
         help="The file to save the generated video to.")
+    parser.add_argument(
+        "--save_latents_file",
+        type=str,
+        default=None,
+        help="Optional path to save the final denoised latents before VAE decode as a .pt file.")
     parser.add_argument(
         "--prompt",
         type=str,
@@ -337,6 +344,23 @@ def generate(args):
         assert not (
             args.ulysses_size > 1
         ), f"sequence parallel are not supported in non-distributed environments."
+        if "ti2v" in args.task and not dist.is_initialized():
+            # Wan2.2-TI2V-5B currently loads sharded diffusers weights through
+            # accelerate, which expects a default process group to exist on
+            # some torch versions even for single-process inference.
+            torch.cuda.set_device(local_rank)
+            os.environ.setdefault("MASTER_ADDR", "127.0.0.1")
+            if "MASTER_PORT" not in os.environ:
+                with socket.socket(socket.AF_INET, socket.SOCK_STREAM) as s:
+                    s.bind(("", 0))
+                    os.environ["MASTER_PORT"] = str(s.getsockname()[1])
+            dist.init_process_group(
+                backend="nccl",
+                init_method="env://",
+                rank=0,
+                world_size=1,
+            )
+            logging.info("Initialized single-process NCCL group for ti2v weight loading.")
 
     if args.ulysses_size > 1:
         assert args.ulysses_size == world_size, f"The number of ulysses_size should be equal to the world size."
@@ -415,7 +439,7 @@ def generate(args):
         )
 
         logging.info(f"Generating video ...")
-        video = wan_t2v.generate(
+        t2v_output = wan_t2v.generate(
             args.prompt,
             size=SIZE_CONFIGS[args.size],
             frame_num=args.frame_num,
@@ -424,7 +448,12 @@ def generate(args):
             sampling_steps=args.sample_steps,
             guide_scale=args.sample_guide_scale,
             seed=args.base_seed,
-            offload_model=args.offload_model)
+            offload_model=args.offload_model,
+            return_latents=bool(args.save_latents_file))
+        if args.save_latents_file:
+            video, latents = t2v_output
+        else:
+            video = t2v_output
     elif "ti2v" in args.task:
         logging.info("Creating WanTI2V pipeline.")
         wan_ti2v = wan.WanTI2V(
@@ -440,7 +469,7 @@ def generate(args):
         )
 
         logging.info(f"Generating video ...")
-        video = wan_ti2v.generate(
+        ti2v_output = wan_ti2v.generate(
             args.prompt,
             img=img,
             size=SIZE_CONFIGS[args.size],
@@ -451,7 +480,12 @@ def generate(args):
             sampling_steps=args.sample_steps,
             guide_scale=args.sample_guide_scale,
             seed=args.base_seed,
-            offload_model=args.offload_model)
+            offload_model=args.offload_model,
+            return_latents=bool(args.save_latents_file))
+        if args.save_latents_file:
+            video, latents = ti2v_output
+        else:
+            video = ti2v_output
     elif "animate" in args.task:
         logging.info("Creating Wan-Animate pipeline.")
         wan_animate = wan.WanAnimate(
@@ -548,6 +582,7 @@ def generate(args):
             args.save_file = f"{args.task}_{args.size.replace('*','x') if sys.platform=='win32' else args.size}_{args.ulysses_size}_{formatted_prompt}_{formatted_time}" + suffix
 
         logging.info(f"Saving generated video to {args.save_file}")
+        Path(args.save_file).parent.mkdir(parents=True, exist_ok=True)
         save_video(
             tensor=video[None],
             save_file=args.save_file,
@@ -555,6 +590,21 @@ def generate(args):
             nrow=1,
             normalize=True,
             value_range=(-1, 1))
+        if args.save_latents_file and args.task in {"t2v-A14B", "ti2v-5B"}:
+            Path(args.save_latents_file).parent.mkdir(parents=True, exist_ok=True)
+            torch.save(
+                {
+                    "task": args.task,
+                    "prompt": args.prompt,
+                    "size": args.size,
+                    "frame_num": args.frame_num,
+                    "fps": cfg.sample_fps,
+                    "seed": args.base_seed,
+                    "latents": latents,
+                },
+                args.save_latents_file,
+            )
+            logging.info(f"Saved latents to {args.save_latents_file}")
         if "s2v" in args.task:
             if args.enable_tts is False:
                 merge_video_audio(video_path=args.save_file, audio_path=args.audio)
diff --git a/wan/__init__.py b/wan/__init__.py
index c49c77e..0b694b2 100644
--- a/wan/__init__.py
+++ b/wan/__init__.py
@@ -1,7 +1,15 @@
 # Copyright 2024-2025 The Alibaba Wan Team Authors. All rights reserved.
 from . import configs, distributed, modules
 from .image2video import WanI2V
-from .speech2video import WanS2V
 from .text2video import WanT2V
 from .textimage2video import WanTI2V
-from .animate import WanAnimate
\ No newline at end of file
+
+try:
+    from .speech2video import WanS2V
+except ModuleNotFoundError:
+    WanS2V = None
+
+try:
+    from .animate import WanAnimate
+except ModuleNotFoundError:
+    WanAnimate = None
diff --git a/wan/modules/attention.py b/wan/modules/attention.py
index 4dbbe03..1f15e18 100644
--- a/wan/modules/attention.py
+++ b/wan/modules/attention.py
@@ -52,6 +52,8 @@ def flash_attention(
     half_dtypes = (torch.float16, torch.bfloat16)
     assert dtype in half_dtypes
     assert q.device.type == 'cuda' and q.size(-1) <= 256
+    q_orig, k_orig, v_orig = q, k, v
+    q_lens_orig, k_lens_orig = q_lens, k_lens
 
     # params
     b, lq, lk, out_dtype = q.size(0), q.size(1), k.size(1), q.dtype
@@ -108,8 +110,7 @@ def flash_attention(
             softmax_scale=softmax_scale,
             causal=causal,
             deterministic=deterministic)[0].unflatten(0, (b, lq))
-    else:
-        assert FLASH_ATTN_2_AVAILABLE
+    elif FLASH_ATTN_2_AVAILABLE:
         x = flash_attn.flash_attn_varlen_func(
             q=q,
             k=k,
@@ -125,6 +126,29 @@ def flash_attention(
             causal=causal,
             window_size=window_size,
             deterministic=deterministic).unflatten(0, (b, lq))
+    else:
+        warnings.warn(
+            'Flash attention is not available, falling back to scaled_dot_product_attention. '
+            'Padding mask and sliding-window behavior are disabled in this fallback.'
+        )
+        q = q_orig.transpose(1, 2).to(dtype)
+        k = k_orig.transpose(1, 2).to(dtype)
+        v = v_orig.transpose(1, 2).to(dtype)
+        if q_scale is not None:
+            q = q * q_scale
+        x = torch.nn.functional.scaled_dot_product_attention(
+            q,
+            k,
+            v,
+            attn_mask=None,
+            dropout_p=dropout_p,
+            is_causal=causal,
+            scale=softmax_scale,
+        ).transpose(1, 2).contiguous()
+        if q_lens_orig is not None or k_lens_orig is not None:
+            warnings.warn(
+                'q_lens/k_lens are ignored in SDPA fallback mode.'
+            )
 
     # output
     return x.type(out_dtype)
diff --git a/wan/text2video.py b/wan/text2video.py
index 7c79c66..c255592 100644
--- a/wan/text2video.py
+++ b/wan/text2video.py
@@ -210,7 +210,8 @@ class WanT2V:
                  guide_scale=5.0,
                  n_prompt="",
                  seed=-1,
-                 offload_model=True):
+                 offload_model=True,
+                 return_latents=False):
         r"""
         Generates video frames from text prompt using diffusion process.
 
@@ -237,6 +238,8 @@ class WanT2V:
                 Random seed for noise generation. If -1, use random seed.
             offload_model (`bool`, *optional*, defaults to True):
                 If True, offloads models to CPU during generation to save VRAM
+            return_latents (`bool`, *optional*, defaults to False):
+                If True, also returns the final denoised latents before VAE decode.
 
         Returns:
             torch.Tensor:
@@ -367,6 +370,8 @@ class WanT2V:
             if self.rank == 0:
                 videos = self.vae.decode(x0)
 
+        final_latents = x0[0].detach().cpu() if (self.rank == 0 and return_latents) else None
+
         del noise, latents
         del sample_scheduler
         if offload_model:
@@ -375,4 +380,8 @@ class WanT2V:
         if dist.is_initialized():
             dist.barrier()
 
-        return videos[0] if self.rank == 0 else None
+        if self.rank != 0:
+            return None
+        if return_latents:
+            return videos[0], final_latents
+        return videos[0]
diff --git a/wan/textimage2video.py b/wan/textimage2video.py
index 67e9fd2..3f4f6ff 100644
--- a/wan/textimage2video.py
+++ b/wan/textimage2video.py
@@ -171,7 +171,8 @@ class WanTI2V:
                  guide_scale=5.0,
                  n_prompt="",
                  seed=-1,
-                 offload_model=True):
+                 offload_model=True,
+                 return_latents=False):
         r"""
         Generates video frames from text prompt using diffusion process.
 
@@ -200,6 +201,8 @@ class WanTI2V:
                 Random seed for noise generation. If -1, use random seed.
             offload_model (`bool`, *optional*, defaults to True):
                 If True, offloads models to CPU during generation to save VRAM
+            return_latents (`bool`, *optional*, defaults to False):
+                If True, also returns the final denoised latents before VAE decode.
 
         Returns:
             torch.Tensor:
@@ -222,7 +225,8 @@ class WanTI2V:
                 guide_scale=guide_scale,
                 n_prompt=n_prompt,
                 seed=seed,
-                offload_model=offload_model)
+                offload_model=offload_model,
+                return_latents=return_latents)
         # t2v
         return self.t2v(
             input_prompt=input_prompt,
@@ -234,7 +238,8 @@ class WanTI2V:
             guide_scale=guide_scale,
             n_prompt=n_prompt,
             seed=seed,
-            offload_model=offload_model)
+            offload_model=offload_model,
+            return_latents=return_latents)
 
     def t2v(self,
             input_prompt,
@@ -246,7 +251,8 @@ class WanTI2V:
             guide_scale=5.0,
             n_prompt="",
             seed=-1,
-            offload_model=True):
+            offload_model=True,
+            return_latents=False):
         r"""
         Generates video frames from text prompt using diffusion process.
 
@@ -271,6 +277,8 @@ class WanTI2V:
                 Random seed for noise generation. If -1, use random seed.
             offload_model (`bool`, *optional*, defaults to True):
                 If True, offloads models to CPU during generation to save VRAM
+            return_latents (`bool`, *optional*, defaults to False):
+                If True, also returns the final denoised latents before VAE decode.
 
         Returns:
             torch.Tensor:
@@ -397,6 +405,7 @@ class WanTI2V:
                 self.model.cpu()
                 torch.cuda.synchronize()
                 torch.cuda.empty_cache()
+            final_latents = x0[0].detach().cpu() if (self.rank == 0 and return_latents) else None
             if self.rank == 0:
                 videos = self.vae.decode(x0)
 
@@ -408,6 +417,8 @@ class WanTI2V:
         if dist.is_initialized():
             dist.barrier()
 
+        if return_latents:
+            return (videos[0], final_latents) if self.rank == 0 else (None, None)
         return videos[0] if self.rank == 0 else None
 
     def i2v(self,
@@ -421,7 +432,8 @@ class WanTI2V:
             guide_scale=5.0,
             n_prompt="",
             seed=-1,
-            offload_model=True):
+            offload_model=True,
+            return_latents=False):
         r"""
         Generates video frames from input image and text prompt using diffusion process.
 
@@ -449,6 +461,8 @@ class WanTI2V:
                 Random seed for noise generation. If -1, use random seed
             offload_model (`bool`, *optional*, defaults to True):
                 If True, offloads models to CPU during generation to save VRAM
+            return_latents (`bool`, *optional*, defaults to False):
+                If True, also returns the final denoised latents before VAE decode.
 
         Returns:
             torch.Tensor:
@@ -605,6 +619,7 @@ class WanTI2V:
                 torch.cuda.synchronize()
                 torch.cuda.empty_cache()
 
+            final_latents = x0[0].detach().cpu() if (self.rank == 0 and return_latents) else None
             if self.rank == 0:
                 videos = self.vae.decode(x0)
 
@@ -616,4 +631,6 @@ class WanTI2V:
         if dist.is_initialized():
             dist.barrier()
 
+        if return_latents:
+            return (videos[0], final_latents) if self.rank == 0 else (None, None)
         return videos[0] if self.rank == 0 else None
